[project]
name = "tractor"
version = "1.0.0"
description = "Export macOS Screen Time data as a daily CSV summary"
requires-python = ">=3.9"
license = "MIT"

[project.scripts]
tractor = "screentime:main"
